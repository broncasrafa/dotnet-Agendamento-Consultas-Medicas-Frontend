import { OrderByLoopCollectionPipe } from './order-by-loop-collection.pipe';

describe('OrderByLoopCollectionPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderByLoopCollectionPipe();
    expect(pipe).toBeTruthy();
  });
});
