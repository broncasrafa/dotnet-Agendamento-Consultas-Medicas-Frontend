import { IdadeFormattedPipe } from './idade-formatted.pipe';

describe('IdadeFormattedPipe', () => {
  it('create an instance', () => {
    const pipe = new IdadeFormattedPipe();
    expect(pipe).toBeTruthy();
  });
});
