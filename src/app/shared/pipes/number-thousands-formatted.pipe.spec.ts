import { NumberThousandsFormattedPipe } from './number-thousands-formatted.pipe';

describe('NumberThousandsFormattedPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberThousandsFormattedPipe();
    expect(pipe).toBeTruthy();
  });
});
