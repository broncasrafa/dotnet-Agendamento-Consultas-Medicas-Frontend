import { DateFormattedPipe } from './date-formatted.pipe';

describe('DateFormattedPipe', () => {
  it('create an instance', () => {
    const pipe = new DateFormattedPipe();
    expect(pipe).toBeTruthy();
  });
});
