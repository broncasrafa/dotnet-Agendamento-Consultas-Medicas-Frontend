import { DateExtensoFormattedPipe } from './date-extenso-formatted.pipe';

describe('DateExtensoFormattedPipe', () => {
  it('create an instance', () => {
    const pipe = new DateExtensoFormattedPipe();
    expect(pipe).toBeTruthy();
  });
});
