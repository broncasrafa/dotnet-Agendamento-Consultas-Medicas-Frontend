import { InputCharacterCountDirective } from './input-character-count.directive';

describe('InputCharacterCountDirective', () => {
  it('should create an instance', () => {
    const directive = new InputCharacterCountDirective();
    expect(directive).toBeTruthy();
  });
});
